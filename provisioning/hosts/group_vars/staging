---
# Write here the vars that are specific to your host
host_name: "staging"
web:
    server_name: "staging.project.com"
iptables_allowed_tcp_ports: [22, 80, 443]

mysql_users:
  - name: "{{ name }}"
    host: "%"
    pass: "{{ name }}"
