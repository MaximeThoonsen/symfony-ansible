---
timezone: Europe/Paris

name: projectname

web:
  port: 80
  server_name: projectname.local

mysql_databases:
  - name: "{{ name }}"

mysql_users:
  - name: "{{ name }}"
    pass: "{{ name }}"
    host: "%"
